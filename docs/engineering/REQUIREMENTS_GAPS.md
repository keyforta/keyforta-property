# Requirements-gap report

This report records decisions without changing requirements.

| Gap                                  | Evidence                                                                                                                                                                      | Potential technical impact                                                                              | Decision owner                           | Status                                               |
| ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ---------------------------------------- | ---------------------------------------------------- |
| External Identity transition timing  | The [Azure customer identity onboarding appendix](../../infra/README.md#customer-identity-onboarding) describes temporary B2B onboarding until External ID exists.                                                                  | Identity claims, onboarding support, redirect registrations, and test environments may change.          | Product owner with security owner        | Product-owner clarification required                 |
| Formal service commitments           | The [proposed service objectives](../operations/README.md#proposed-service-objectives) label current targets as pilot learning targets.                                                                                                   | Alert thresholds, capacity, support coverage, HA, and cost cannot be finalized.                         | Product owner with SRE owner             | Approval required before external commitment         |
| Record-specific retention periods    | `docs/engineering/THREAT_MODEL.md#appendix-a-supporting-security-architecture` identifies sensitive classes but not approved retention/deletion periods per class.              | Storage lifecycle, legal holds, deletion workflow, backups, and privacy responses remain provisional.   | Product owner with privacy/legal owner   | Product-owner clarification required                 |
| AI feature activation                | `docs/product/IMPLEMENTATION_PLAN.md` defers grounded assistance and `docs/ai/EVALUATION_PLAN.md` requires release evidence. [Issue #15](https://github.com/keyforta/keyforta-property/issues/15) and ADR-012 approve a bounded, non-production, authenticated read-only MCP foundation. | Grounded assistance, retrieval, business tools, model calls, and autonomous decisions must remain inactive. | Product owner with AI/security reviewers | Synthetic-only MCP implementation and ChatGPT-first dev activation at `mcp.keyforta.com` approved with a USD 25 monthly ceiling; exact identity, protected plan, DNS/TLS, monitoring, evidence, and rollback gates remain required |
| Production topology and availability | ADR-0006 authorizes a lean single dev environment and defers production decisions.                                                                                            | HA, private networking, staging, RTO/RPO validation, and budget cannot be implemented safely.           | Product owner with platform/SRE owner    | Consequential ADR and approval required              |
| Authenticated DAST identity           | Repository DAST exercises the synthetic loopback API without Entra credentials; authorization and cross-organization behavior remain covered by API integration tests.       | End-to-end dynamic authorization testing requires an approved synthetic issuer/JWKS, identities, organization fixtures, and token-handling policy. | Product owner with security and QA owners | Identity design and activation approval required     |
| Artifact signing and deployment-result evidence | Plan-time BuildKit SBOM/provenance and digest evidence are retained, but no approved signing identity/trust policy or single post-deployment result artifact exists.       | Signature verification, retention, deployed revision capture, and release attestation cannot be standardized safely. | Security owner with platform/SRE owner | Trust policy and evidence-retention decision required |
| Accessibility automation standard    | Existing web tests do not select a browser accessibility runner.                                                                                                              | Automated WCAG regression coverage and CI duration remain undefined.                                    | Engineering owner with UX reviewer       | Tooling approval required; manual review remains     |
| GitHub required-review enforcement   | GitHub reports zero required approvals and plan-level `403` responses for protection APIs; issue #28 retires the custom verifier and keeps GitHub reviews as the sole source. | Reviews remain voluntary and merges are not technically blocked on approval.                            | Repository owner                         | Accepted temporary limitation; enable when available |
| Merged topology and ADR reconciliation | Supporting documents under `docs/architecture/` describe a lean single-environment pilot and use a second ADR numbering series, while the authoritative backend specification, production data model, and `docs/adr/` retain broader environment and architecture contracts. | Environment promotion, HA, retention, database migration ownership, and deployment scope cannot be treated as superseded without an explicit decision. | Product owner with architecture, data, and SRE owners | Merged as supporting material; reconciliation and explicit approval required |
| Executable database migration authority | `docs/contracts/keyforta-production-data-model.md` describes the target production model while deployment and the checksummed runner execute `infra/postgres/migrations`. | A big-bang replacement would break the migration ledger and existing data; target-model gaps must be delivered as additive forward migrations. | Product owner with architecture and data owners | Resolved 2026-09-16 by ADR-013: operational forward migrations are canonical executable history; the production model remains the normative target design |
| Source API migration authority | Source revision `39eab863e83d684c113f16f33e0a3b92f9ee8188` exposes route, authentication, organization-context, payment, and persistence contracts that differ from `docs/openapi.yaml` and the authoritative backend specification. [Issue #1](https://github.com/keyforta/keyforta-property/issues/1) approves a full migration through bounded adaptation slices. | A direct import could weaken organization isolation, change public contracts, or violate immutable financial-history rules. The runtime must be adapted in bounded slices rather than renamed wholesale. | Product owner with architecture, security, data, and backend owners | Umbrella scope approved; slice-specific plans and independent reviews required |
| Public listing publication authority and visibility | REQ-003 requires explicit publication approval, while the current public projection trusts listing status without proving actor authority or the property and unit lifecycle state. | Unauthorized or stale publication could expose organization inventory publicly. | Product owner with security and data owners | Superseded by REQ-003 v2: a property contains units; only its single active assigned listing manager may manage unit listings; the landlord may assign themselves or another eligible same-organization member; ownership alone grants no listing authority |
| Empty-start landlord onboarding | The current development seed creates demonstration inventory and the admin console uses a browser-local mock identity, so no trusted workflow can admit the first landlord into an empty environment. | Removing fixtures without an authenticated application and approval path would leave the product unusable; retaining them would violate the requested clean start. | Product owner with security and data owners | Resolved 2026-09-16 by REQ-028 through REQ-031: no demo/customer seed data; verified applicants submit after sign-in; allowlisted Entra platform administrators approve; approval atomically creates the organization and landlord membership |
| Verification lifecycle vocabulary | `docs/contracts/keyforta-state-machines.md` defines property and operator review with `submitted`, `under_review`, and `approved` or `verified`, while the normative target SQL `verification_status` enum uses `pending`, `changes_requested`, and `verified` and does not include `submitted`, `under_review`, or `approved`. | Implementations and migrations cannot safely map lifecycle commands, persisted values, and public projections until one vocabulary or an explicit translation is approved. | Product owner with domain, data, and API owners | Proposed resolution (pending product-owner ratification, tracked in issue #85): `keyforta-state-machines.md` §2 and §8 were rewritten to use the SQL enum vocabulary (`not_started`, `pending`, `changes_requested`, `verified`, `suspended`, `rejected`, `expired`) since the SQL enum is the target persistence layer and no runtime code currently depends on the prior `submitted`/`under_review`/`approved` wording. Note: `packages/contracts/src/index.js`'s `rentalPropertySchema` currently accepts `verificationStatus` as free-form bounded text, not validated against either enum, so this is not yet enforced anywhere — it is a documentation-only proposal. Product owner must confirm this direction, and API/contracts owners must add enum validation, before it is treated as final. |
| Portfolio concept ownership | Supporting domain views use `Portfolio`, but the normative context ownership matrix and production table inventory do not assign it as an aggregate or persisted entity. | Implementations may create an unnecessary aggregate/table or duplicate a read-model scope. | Product owner with domain and data owners | Proposed resolution (pending product-owner ratification, tracked in issue #85): `keyforta-context-map-and-ownership.md` §"Property & Inventory" now states Portfolio is a derived, query-time scope over `Property` rows (joined through Party & Relationship ownership/management facts), used both as an authorization scope (per AUTH-004's delegated-portfolio boundary) and as a Reporting-context read model — not an aggregate, table, or persisted entity in either use. No `Portfolio` table/aggregate/ID should be created. Product owner must confirm this direction before it is treated as final. |
| Product authority and invited-application scope | The approved PRD includes invitation-only tenant applications and excludes public applications; the draft role use cases and historical blueprint contain broader “future” or excluded application statements while backend design documents describe other entry triggers. | Onboarding, authorization, evidence collection, and lease provenance cannot share one acceptance boundary until the product authority chain and invitation trigger are reconciled. | Product owner with domain, UX, security, and API owners | Product-owner reconciliation required; PRD scope controls provisionally and draft/historical documents must not broaden it |
| Public rental-application prototype | `apps/public-web` exposes a browser-local `/apply/:propertyId` prototype. The PRD amendment (issue #75, 2026-09-18) now permits public rental applications, but this prototype still stores personal/income data client-side only, with no durable API persistence, human-review gate, or abuse controls. | Users may mistake local storage of personal and income data for an approved application workflow; release and privacy claims could be false. | Product owner with UX, privacy, and security owners | Ratified direction: re-scope (not remove) as a real API-backed flow with a human-review gate (issue #73). Implementation deferred per product-owner instruction (2026-09-18) until dependent decisions (public-application abuse-control policy, and #79/#81/#83) are settled; status ledger marks the route non-authoritative until then. |
| Pre-launch v1.0 compatibility cleanup | The Product Owner confirmed there is no launched client or production/customer dataset to preserve. Audits found compatibility-only public field aliases, route adapters, synthetic listing expectations, legacy publication states, identity dual-write fields, and unconditional N-1 rollout assumptions across active contracts, API, web, persistence, tests, and operations documentation. | Carrying these paths into first release increases implementation and security complexity and can preserve unapproved behavior. Removing them without ownership boundaries could instead damage migration history, auth, or deployment recovery. | Product owner with architecture, API, data, frontend, security, QA, and SRE owners | Remove each compatibility-only path in its owning reviewed slice before v1.0 release; current rental-contract work removes transitional profiles and unsupported listing states. API/public projection, public-web routes/application prototype, database/reset, identity, and rollout cleanup remain separately tracked implementation work. Preserve checksummed forward migrations, immutable history, provider interoperability, and fail-closed controls. |
| HTTP wire contract and response authority | `docs/openapi.yaml`, the backend implementation specification, API/event contract, backend integration contract, shared schemas, and current Fastify routes disagreed on paths, money fields, collection/error envelopes, and generic mutation schemas. | Clients and tests cannot safely infer one public contract; generic updates could weaken lifecycle, authorization, and minor-unit money invariants. | Product owner with API, domain, security, and frontend owners | Partially resolved for implemented runtime routes: operations marked `x-keyforta-runtime: true` in OpenAPI are the named wire authority, shared schemas export the same implemented route manifest/envelopes, and tests compare that operation inventory bidirectionally and verify runtime authentication behavior. Remaining schema-level parity, unimplemented route inventory, public unit application path, onboarding/signup consolidation, and total-count policy require product-owner clarification before implementation. |
| Signed 64-bit money on the JSON wire | REQ-034 permits positive signed 64-bit minor-unit values while the approved OpenAPI wire represents `Money.amountMinor` as a JSON integer and explicitly defers a lossless base-10 string contract. JavaScript numbers cannot losslessly represent every signed 64-bit integer. | Shared JSON schemas must reject unsafe integers; API clients cannot transport the full database range until a versioned lossless representation is approved. | Product owner with domain and API owners | Product-owner clarification required before exposing amounts above `Number.MAX_SAFE_INTEGER`; database storage remains signed 64-bit and the current wire remains numeric. |
| Jurisdiction policy catalogue | REQ-032 requires `jurisdictionCode` before verification, but no approved policy catalogue, identifier format, or lifecycle exists. The v1.0 shared Property contract therefore rejects this field and verified/published Property states rather than accepting arbitrary legal-policy values. | Property creation can proceed privately, but verification and publication remain blocked until a catalogue and verification mapping are approved. | Product owner with legal/privacy, domain, data, and API owners | Catalogue approval required before adding `jurisdictionCode` and verified/published states to the shared complete Property contract or persistence schema. |
| Target SQL composition and jurisdiction defaults | Reference `V002` applies an organization policy to a `V001` organization table without `organization_id`; `V001` also defaults jurisdiction/policy values that the production model and operational migrations leave unselected pending approval. | The target reference composition is not executable and could seed an unapproved legal conclusion if mistaken for deployment input. | Product owner with data, legal/privacy, and security owners | Data/legal clarification required; only `infra/postgres/migrations` is executable authority |
| Requirement and acceptance identifier crosswalk | Product role requirements use `REQ-*`, while acceptance traceability uses domain-prefixed IDs and executable tests do not consistently carry either identifier. | Coverage and release evidence cannot be followed deterministically from approved requirement to test and gate. | Product owner with QA and engineering owners | Add an approved many-to-many crosswalk and executable test references; do not renumber either set silently |
| Public rental-application abuse-control policy | Issue #75 ratified (2026-09-18) that rental applications may be submitted by the public (unauthenticated), not only invited tenants. This mirrors the PROP-023 public-catalogue exposure but no abuse-control policy (rate limiting, duplicate/spam suppression, identity-verification depth, enumeration protection) has been approved for public application submission. | Building the public application API without an approved abuse-control policy risks spam/fraud volume and unbounded review workload for landlords/managers. | Product owner with security, domain, and API owners | Not yet proposed. Must be approved before the public application endpoint accepts unauthenticated submissions in a released environment; every application still requires an explicit human review decision regardless of source. |
| Property and inventory entity depth | The approved [Rental Property and Unit Core Profile v1.0](../product/RENTAL_PROPERTY_INVENTORY_REQUIREMENTS_PROPOSAL.md) defines production field profiles, provenance, visibility, localization, lifecycle validation, and acceptance criteria for Property, Unit, PricingVersion, availability, and controlled public listings. | Delivery requires contracts, forward migrations, APIs, interfaces, authorization, privacy controls, and executable evidence. Pre-launch synthetic data is not a compatibility boundary and may be reset only through the approved guarded process. | Product owner with domain, data, UX, privacy, and API owners | Resolved 2026-09-17 by REQ-032 through REQ-036 and PROP-009 through PROP-023, clarified before launch within v1.0; amenities, new media activation, retention periods, jurisdiction and verification catalogues, and the external-beta abuse-control policy remain separately gated; sales, valuation, mortgage, MLS, and neighborhood-data products remain excluded |

Agents must continue requirement-independent work and must not resolve these rows
by changing product behavior.

- 2026-09-18: Issue #69 scope includes `apps/jobs`, but the package is currently a scaffold with no runtime source. Implemented boundary tests for the existing `check` and `build` scripts instead of inventing worker logic; future behavioral coverage should begin once real job handlers exist.
