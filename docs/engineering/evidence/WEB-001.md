# WEB-001 public landing page redesign

## Scope

Task `WEB-001` redesigns the public homepage without changing authentication,
authorization, persistence, or the public listings contract. The search form
continues to submit `district`, `maxRent`, and `bedrooms` to `/locations`.

## Tests

`apps/public-web/test/public-homepage.test.ts` verifies the approved search parameters,
public entry points and navigation, first-viewport product signals, and stable
responsive search and action layouts.

## Results

The web test suite, lint, TypeScript checks, root production build, and canonical
verification pass. The search form preserves its `/locations` query contract.

## Coverage

Regression tests cover the homepage route, search fields, public navigation,
first-viewport product signals, and stable responsive action layouts.

## Security

No authentication, authorization, persistence, or public data-access behavior
changes. Existing public boundaries remain unchanged.

## Accessibility

Keyboard navigation reaches public navigation, sign-in, search controls, the
hero sign-in link, and the primary action in document order with visible focus.
Desktop, tablet, and mobile layouts keep the search action unobscured.

## Architecture

The change remains within the Next.js web application and consumes the existing
public contract without duplicating domain or persistence logic.

## Infrastructure

No infrastructure behavior changes. Existing Bicep compilation remains part of
canonical verification.

## Repair cycle 1

GitHub Actions run `34710084065` passed source, test, build, and infrastructure
gates but rejected the evidence manifest because WEB-001 remained at
`implementation-ready` after implementation and verification had completed.
The bounded correction advances the task history through the completed states;
no product behavior or quality gate was changed.

## Repair cycle 2

Review findings identified a role-specific label on the shared sign-in route,
an overflowing tablet search grid, and a mobile selector that could not match
the first label. The bounded correction uses neutral sign-in copy, adds a
three-column tablet layout with a full-width submit row, targets the first label
by type on mobile, and adds regression assertions for these behaviors. Browser
checks at 769x600 and 800x600 confirmed three equal input columns, a fully
visible submit button, and no horizontal overflow; desktop and mobile checks
also kept the search action visible without horizontal overflow.
