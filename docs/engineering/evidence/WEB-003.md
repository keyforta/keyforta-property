# WEB-003: KEYFORTA brand integration successor

## Authority

- Approved issue: https://github.com/cmbuyamba/keyforta-property/issues/43
- Task contract: `harness/tasks/keyforta-brand-successor.json`
- Assigned route: `ROUTE-004`
- Blocked predecessor audit: commit `85c1e4d` on `feature/keyforta-brand`

## Scope

Complete the approved public product rename to KEYFORTA across the web
experience and current product documentation. Preserve migration-sensitive
technical identifiers and use `contact@keyforta.com` as the public fallback
address.

WEB-003 is a clean successor to terminally blocked WEB-002. It carries only the
reviewed implementation; it does not rewrite or discard the predecessor's
failure history.

## Implementation

- Added a reusable `BrandLogo` component backed by approved design-kit assets.
- Published primary, symbol, wordmark, full monochrome, monochrome symbol,
  monochrome wordmark, reversed, and app-icon PNGs byte-for-byte from the
  supplied kit.
- Applied KEYFORTA branding to public, authentication, invitation, landlord,
  manager, and tenant surfaces.
- Mapped semantic CSS and Fluent UI brand roles to Aubergine, Mineral Teal,
  Burnished Copper, and Soft Bone.
- Updated metadata, the file-based application icon, current product and
  architecture documentation, and public contact copy.
- Added regression coverage for logo variants, source-to-runtime asset parity,
  responsive sizing, navigation adoption, metadata, icon integrity, and legacy
  public text.

## Preserved identifiers

The Entra audience `api://keyforta`, harness schema identifiers, historical
task evidence, demonstration seed data, package names, routes, database objects,
and infrastructure resource names remain unchanged.

## Tests

`apps/public-web/test/brand-logo.test.tsx` verifies all seven reusable logo variants,
intrinsic dimensions, accessible text, byte-identical source and runtime assets,
responsive header and footer behavior, navigation adoption, metadata, app-icon
integrity, and removal of the legacy public name. The complete web suite covers
53 tests across ten files.

## Results

The task contract, formatting, lint, typecheck, unit and contract tests,
production build, required scenarios, and evidence checks pass locally. The
predecessor implementation and WEB-003 repair history remain preserved in
WEB-002 and commit `85c1e4d`.

The final pre-review canonical run with WEB-003 in terminal `verified` state
completed at `2026-09-13T00:45:00.123Z` with status `passed`,
`nextState: verified`, and no failed gates. Review repairs added the omitted
monochrome variants and runtime parity coverage and restored valid requirement
anchors.

## Coverage

Automated coverage includes every approved display asset, public and workspace
navigation, authentication and invitation surfaces, metadata, the application
icon, responsive logo sizing, current user-facing documentation, and legacy
public-name removal. Manual browser review covered 320 px, 768 px, and desktop
layouts.

## Security

No authentication, authorization, session, persistence, or data-access behavior
changes. Migration-sensitive identifiers remain unchanged, and secret scanning
and dependency auditing remain part of canonical verification.

## Accessibility

The shared logo component supplies meaningful alternative text and stable
intrinsic dimensions. Keyboard focus, contrast, responsive navigation, and logo
readability were reviewed across mobile, tablet, and desktop layouts.

## Architecture

Brand presentation remains owned by the Next.js web application. Approved source
assets stay in the design kit and byte-identical runtime copies stay under
`apps/public-web/public`; no domain, persistence, or transport boundary changes.

## Infrastructure

No infrastructure files or behavior change. All six Bicep files compile locally.
GitHub CI runs `34728824428` and `34730059439` also passed the infrastructure gate
but failed the subsequent evidence-manifest gate because this document lacked the
required structured sections. A fresh exact-head GitHub CI run is required before
merge.
