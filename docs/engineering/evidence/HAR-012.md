# HAR-012 complete PR 13 CI validation

## Scope

Complete the existing PR #13 without creating another external issue or pull
request. HAR-011 remains blocked and unchanged after its exhausted repair cycle.

## Failure

GitHub run `35042167238` executed the self-test inside the real `keyforta-ci`
launcher. The regression then invoked nested `sudo`, which correctly failed
because the producer has no sudo authority.

## Bounded correction

When the self-test is already running as the configured validation user in CI,
inspect its inherited allowlisted environment directly. Retain local execution
of the complete nested launcher. Preserve blocked-transition evidence generation
and require one exact package step between producer shutdown and upload.

## Repair cycle

Cycle one added the policy-required `Tests` section after clean-head evidence
validation rejected its absence. Cycle two addresses independent review findings
by requiring live cgroup membership and the exact validation-job step sequence.

## Acceptance criteria

- The CI producer never receives sudo authority.
- The live launcher environment contains required values and no ambient secret.
- The package candidate step remains unique and directly ordered after shutdown.
- Exact-head CI and protected retention pass before merge.

## Verification

- `pnpm harness:self-test`
- `HARNESS_TASK_CONTRACT=harness/tasks/HAR-012.json pnpm verify:task`
- `HARNESS_TASK_CONTRACT=harness/tasks/HAR-012.json pnpm verify:classification`
- `CI=true HARNESS_TASK_CONTRACT=harness/tasks/HAR-012.json pnpm verify:all`

## Evidence references

- https://github.com/keyforta/keyforta-property/actions/runs/35042167238
- `harness/reports/verify-latest.json`
- `docs/engineering/evidence/HAR-012.md`

## Results

The focused lifecycle suite passes. It verifies the live CI launcher environment
without nested sudo, retains local launcher execution, rejects `/bin/tar` and
escaped `\tar` package steps, and requires the package step to sit directly
between producer shutdown and upload.

## Tests

- `pnpm harness:self-test` passed.
- `HARNESS_TASK_CONTRACT=harness/tasks/HAR-012.json pnpm verify:task` passed.
- `HARNESS_TASK_CONTRACT=harness/tasks/HAR-012.json pnpm verify:classification`
  passed.
- The first clean-head canonical run reached evidence validation and requested
	this policy-required section; repair cycle one adds it without changing code.