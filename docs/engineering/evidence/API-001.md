# API-001: KEYFORTA invitation email copy

## Authority

- Approved issue: https://github.com/cmbuyamba/keyforta-property/issues/43
- Task contract: `harness/tasks/keyforta-invitation-copy.json`
- Assigned route: `ROUTE-005`

## Scope

Replace the legacy product name in the Microsoft Graph B2B invitation message
with KEYFORTA. Preserve the recipient, acceptance token and URL, French locale,
correlation ID, authorization header, and failure behavior.

## Verification

- The regression assertion failed against the legacy message before the
  production change, confirming the controlling behavior.
- The complete API suite passed 71 tests; 18 PostgreSQL integration tests were
  skipped because `DATABASE_URL` is absent.
- The invitation payload test verifies the exact French KEYFORTA message while
  retaining the recipient, redirect URL, locale, and send flag assertions.
- Canonical repair cycle 1 passed every executable gate, then failed evidence
  manifest generation because this document lacked the required `Tests`
  section. This acceptance test map was added before rerunning verification.
- GitHub CI run `34729127132` for head `26d81e1` passed repository, test,
  build, and infrastructure checks, then failed evidence-manifest validation
  because this report lacked the required `Results`, `Coverage`, `Security`,
  `Accessibility`, `Architecture`, and `Infrastructure` sections.
- Those six sections were added without changing application behavior.

## Tests

| Acceptance criterion | Test                                        | Result                          |
| -------------------- | ------------------------------------------- | ------------------------------- |
| `AC-API-001`         | `apps/api/test/invitation-delivery.test.ts` | Passed in the 71-test API suite |

## Results

- The invitation message now names KEYFORTA and preserves the recipient,
  redirect URL, locale, send flag, authorization header, and correlation ID.
- API lint, typecheck, tests, and the production build passed.
- The final local canonical verification completed with status `passed` and
  `nextState: verified`.

## Coverage

- The focused regression test asserts the exact French invitation message and
  the unchanged Microsoft Graph request fields.
- Existing API coverage passed 71 tests; 18 PostgreSQL integration tests were
  skipped locally because this copy-only change does not require a database and
  `DATABASE_URL` was absent from that focused run.

## Security

- No authentication, authorization, token, redirect, or delivery behavior was
  changed.
- The regression test preserves the bearer authorization header and opaque
  invitation token handling.

## Accessibility

- This change affects plain-text invitation copy only and introduces no web UI
  or interactive accessibility surface.

## Architecture

- The change remains within the API invitation-delivery boundary and adds no
  dependency or cross-module behavior.

## Infrastructure

- No infrastructure or deployment configuration changed.
- Canonical verification successfully built all six Bicep files.
