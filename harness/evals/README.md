# Evaluations

Current evaluations are deterministic repository checks implemented in
`../graders/` and exercised by `../scripts/self-test.mjs`. AI feature evaluations
remain governed by `docs/ai/EVALUATION_PLAN.md` and are not activated until an
approved AI feature task provides versioned scenarios, expected evidence, and a
safe-fallback test.
